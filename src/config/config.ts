// Correctly import the JSON data
import config from './config.json'

export const siteMetadata = config.site;
export const settings = config.settings;
export const params = config.params;
export const navigationButton = config.navigation_button;
export const disqus = config.disqus;
export const siteUrl = config.site.base_url;
export const metadata = config.metadata;
